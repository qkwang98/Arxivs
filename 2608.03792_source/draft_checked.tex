\documentclass[11pt]{article}

\input{preamble.tex}
\usepackage{fullpage}
\usepackage{ytableau}
\usepackage{mathabx}
%\usepackage[landscape]{geometry}
\usepackage{array}
\usepackage{colonequals}

\def\ModG{\mathsf{Mod}_G}
\def\ModH{\mathsf{Mod}_H}
\def\TT{{\mathbb T}}
\def\Ad{\mathrm{Ad}}
\def\SS{{\mathbb{S}}}

\def\W{W^{\BC}}
\def\WD{W^{\D}}
\def\e{\mathbf{e}}
\def\abs{\mathsf{abs}}

\graphicspath{{figures/}}


\begin{document}
\title{Molecules of an affine FPF $W$-graph and an asymptotic row-Beissinger correspondence}
\author{
Yifeng ZHANG\thanks{
Email: \tt calvinz314159@gmail.com
}
}
\date{South China Normal University}



\maketitle

\begin{abstract}
Kazhdan--Lusztig $W$-graphs encode the cell structure of Hecke algebras, while their bidirected connected components are called molecules.
In finite type~$A$, the Robinson--Schensted correspondence describes cells and molecules, and Beissinger's row insertion constructs the common tableau associated with an involution.
In affine type~$A$, the affine matrix-ball construction assigns an affine permutation a pair of tabloids together with a dominant weight, and Marberg introduced affine FPF $W$-graphs indexed by affine fixed-point-free involutions.
We prove that, for an affine fixed-point-free involution, complete two-cycle truncations followed by finite row Beissinger insertion asymptotically recover both its common AMBC tabloid and its dominant weight.
We also identify the bidirected edges of $\Gamma_n^{\m}$ with dual equivalence moves under AMBC, obtaining a classification of its molecule.

\end{abstract}

\setcounter{tocdepth}{2}
\tableofcontents

\section{Introduction}

Kazhdan--Lusztig theory associates to a Coxeter system
$(W,S)$ a canonical basis of its Hecke algebra and a
corresponding $W$-graph \cite{KL}.  The strongly connected
components of this graph are the Kazhdan--Lusztig cells, while
the connected components obtained by retaining only the
bidirected edges are called molecules.

For the symmetric group, the Robinson--Schensted
correspondence gives a combinatorial description of this
structure.  The insertion and recording tableaux determine the
right and left cells, respectively, while Knuth moves describe
the bidirected edges.  When the input permutation is an
involution, its two Robinson--Schensted tableaux coincide.
Beissinger's insertion algorithm constructs this common
tableau directly from the cycles of the involution
\cite{Beissinger}.

For the affine symmetric group $\tS_n$, Chmutov,
Pylyavskyy, and Yudovina introduced the affine matrix-ball
construction
\[
\Phi:\tS_n\longrightarrow\dom,
\qquad
w\longmapsto\bigl(P(w),Q(w),\rho(w)\bigr),
\]
where $P(w)$ and $Q(w)$ are tabloids of the same shape and
$\rho(w)$ is a dominant weight \cite{CPY}.  The behavior of
this correspondence under affine Knuth moves and dual
equivalence was further studied by Chmutov, Lewis, and
Pylyavskyy \cite{CLP}.

Marberg constructed two $\tS_n$-graphs whose vertices are the
affine fixed-point-free involutions in $\cF_n$ \cite{Marberg}.
In this paper, we study the molecules of the graph
$\Gamma_n^{\m}$.  If $x\in\cF_n$, then
\[
\Phi(x)=(P,P,\rho),
\]
so the two tabloid coordinates of AMBC coincide.

Our first main result gives an insertion-theoretic realization
of this AMBC datum.  We truncate the periodic matching of
$x$ by taking complete two-cycles and apply finite row
Beissinger insertion.  We prove that the resulting tableaux
stabilize asymptotically: their stable rows recover the AMBC
tabloid $P$, and the blockwise normalization of their
horizontal phase vector recovers the dominant weight $\rho$.
Thus the AMBC data of an affine fixed-point-free involution can
be recovered from finite row Beissinger tableaux.

Our second main result describes the molecules of
$\Gamma_n^{\m}$.  We identify its bidirected edges with
involutive transformations, which correspond under AMBC to
dual equivalence moves on the common tabloid while preserving
the dominant weight. Therefore, we have the main thoerem.
\begin{theorem}[Classification and enumeration of molecules]
\label{intro-molecule-thm}
Let $\lambda$ be an AMBC shape occurring for an element of $\cF_n$, and
let $\rho$ be a dominant weight occurring with $\lambda$.
\begin{enumerate}
\item The molecules of $\Gamma_n^{\m}$ with AMBC shape $\lambda$ and
      weight $\rho$ are indexed by the dual-equivalence components of
      $\RSYT(\lambda)$, and there are exactly $d_\lambda$ of them.
\item All molecules of $\Gamma_n^{\m}$ having the same AMBC shape are
      isomorphic as edge-labeled graphs.
\end{enumerate}
\end{theorem}

The paper is organized as follows.  Section~\ref{prelim-sect} recalls affine
permutations, tabloids, AMBC, and Marberg's affine FPF $W$-graphs.
Section~\ref{Knuth-sect} introduces affine Knuth moves and involutive transformations.
Section~\ref{shift-sect} studies their compatibility with the cyclic shift.  In
Section~\ref{sec:affine-rb} we construct the complete two-cycle row Beissinger
truncations, prove stabilization, and recover both the AMBC tabloid and
the dominant weight.  Section~\ref{mole-sect} identifies the bidirected edges of
$\Gamma_n^{\m}$ and derives the classification, enumeration, and
symmetry results for its molecules.


\section{Preliminary}\label{prelim-sect}

\subsection{Affine permutations and involutions}
Let $n$ be a positive integer. Write $\ZZ$ for the set of integers and define $[n] = \{1,2,\dots,n\}$.
Let $\NN = \{0,1,2,\dots\}$ and $\PP=\{1,2,3,\dots\}$ be the sets of nonnegative and positive integers.

\begin{definition}
The \defn{affine symmetric group} $\tS_n$ is the group of bijections $\pi:\ZZ \to \ZZ$
satisfying $\pi(i+n) = \pi(i)+n$ for all $i \in \ZZ$ and $\pi(1) + \pi(2) + \dots +\pi(n) = 1 + 2 + \dots +n$.
\end{definition}

We refer to elements of $\tS_n$ as \defn{affine permutations}.
A \defn{window} for an affine permutation $\pi \in \tS_n$ is a sequence of the form $[\pi(i+1), \pi(i+2),\dots,\pi(i+n)]$ where $i \in \ZZ$.
An element $\pi \in \tS_n$ is uniquely determined by any of its windows,
and a sequence of $n$ distinct integers is a window for some $\pi \in\tS_n$ if and only if the integers represent
each congruence class modulo $n$ exactly once.

Let $s_i $ for $i \in \ZZ$  be the unique element of $\tS_n$ that interchanges $i$ and $i+1$ while fixing every integer $j \notin \{i,i+1\} + n\ZZ$.
One has $s_i = s_{i+n}$ for all $i \in \ZZ$, and 
 $\{s_1,s_2,\dots,s_n\}$ generates the group $\tS_n$.
With respect to this generating set, $\tS_n$ is the affine Coxeter group of type $\tilde A_{n-1}$.
The parabolic subgroup $S_n = \langle s_1,s_2,\dots,s_{n-1}\rangle \subset \tS_n$
is the finite Coxeter group of type $A_{n-1}$; its elements
are
the permutations $\pi \in \tS_n$ with $\pi([n]) = [n]$.

A \defn{reduced expression} for $\pi \in \tS_n$ is a minimal-length factorization $\pi = s_{i_1}s_{i_2}\cdots s_{i_l}$.
The \defn{length} of $\pi \in \tS_n$, denoted $\ell(\pi)$, is the number of factors  in any of its reduced expressions.
The value of $\ell(\pi)$ is also the number of equivalence classes in the set
$
\Inv(\pi) = \{ (i,j) \in \ZZ \times \ZZ : i< j\text{ and }\pi(i)> \pi(j)\}
$
under the relation $\sim$
on $\ZZ\times \ZZ$ with $(a,b) \sim (a',b')$ if and only if $a-a' = b-b' \in n \ZZ$.

From here, let $n$ be an even integer. An affine involution is $z\in\tS_n$ such that $z^2=1$. An affine fixed-point-free involution is an affine involution $z$ such that there are no $x\in[n]$ with $z(x)=x$. The set of all involutions is denoted as $\tilde\cI_n$ while the set of all fixed-point-free involutions is denoted as $\cF_n$. On $\cF_n$, we define $\ellfpf(z) = \tfrac{1}{2}(\ell(z) - \tfrac{n}{2})$.

\begin{definition}
Given $\pi \in\tS_n$, define $\beta(\pi)=\frac{1}{2n}\sum_{i=1}^n|\pi(i)-r_n(\pi(i))|,$ where $r_n(i)$ for $i \in \ZZ$ denotes the unique element of $[n]$ that satisfies $r_n(i)\equiv i\pmod n$. For $z \in \cF_n$, define $\sfpf(z)=(-1)^{\beta(z)}$.
\end{definition}
Let $\tp =s_1s_3\cdots s_{n-1}= [2,1,4,3,\dots,n,n-1] \in \tI_n$ and 
$\Theta^-= s_2s_4\cdots s_n= [1,0,3,2,\dots,n-1,n-2]  \in \tI_n$,
so that $\sfpf(\Theta^\pm)=\pm 1$.
We reserve the symbol $\Theta$ to denote one of the two elements of $\{ \Theta^\pm\}$.
Define $\cFp$ as the $\tS_n$-conjugacy class of $\Theta^+$ and $\cFm$ as the $\tS_n$-conjugacy class of $\Theta^-$. One can show that
\be\label{+-eq} \cFp =\{z\in\cF_n : \sfpf(z)=1\} \qquand
 \cFm =\{z\in\cF_n : \sfpf(z)=-1\}\ee
and hence that $\cF_n=\cFp\sqcup\cFm$; see, e.g., \cite[Theorem 5.4]{Marberg2}.


\subsection{Tabloids}
Let $\lambda = (\lambda_1, \ldots, \lambda_{\ell})$ be a partition of size $\sum_i \lambda_i \leq n$. A \defn{tabloid} $P$ of shape $\lambda$ is an equivalence class of fillings of the Young diagram of shape $\lambda$ with elements of $[\ol{n}]$ under identification of fillings that differ by reordering  elements within rows. Here $\ol{i}$ denotes the equivalence class of integers $k\equiv i\pmod{n}$.
We think of the $i$-th row of a tabloid $P$ as a set $P_i\subseteq[\ol{n}]$.

\begin{example}
\[
\yttab{\ol{1}&\ol{2}&\ol{3} \\ \ol{4}} \qquad 
\yttab{\ol{3}&\ol{1}&\ol{2} \\ \ol{4}} \qquad 
\yttab{\ol{4}&\ol{2}&\ol{3}\\ \ol{1}} \qquad 
\yttab{\ol{4}&\ol{1}&\ol{2} \\\ol{3}}\qquad 
\yttab{\ol{1}&\ol{4}&\ol{3}\\ \ol{2}} \qquad 
\]
These are several tabloids of shape $(3,1)$.  The first two tabloids are equal, since they differ only by permuting elements within rows.
\end{example}

The order of elements in each row does not matter, but it is convenient to regard elements of $T_i$ as being ordered with respect to the linear ordering $\ol{1} < \cdots < \ol{n}$. In this case we say that $T$ is presented \defn{row-standard}. In the remaining sections, we assume all tabloids are presented row-standard. For a partition $\lambda\vdash n$, define $\RSYT(\lambda)$ to be the set of row-standard-presented Young tabloid of shape $\lambda$.


We call the analogue of the descent set for tabloids the \defn{$\tau$-invariant}, in reference to Vogan's (generalized) $\tau$-invariant \cite{Vogan}.
\begin{definition}
For a tabloid $T$ filled with all the elements of $[\ol{n}]$, define the \defn{$\tau$-invariant} by
\[
\tau(T) := \{\ol{i}\in [\ol{n}]: \ol{i} \text{ lies in a strictly higher row of } T \text{ than } \ol{i + 1}\}.
\]
\end{definition}
\begin{example}
\label{ex:tabloid tau}
The following tabloid  $T$ has $\tau(T) = \{\ol{2},\ol{5}, \ol{8}\}$.
\[
T := \yttab{\ol{2}&\ol{4}&\ol{5}\\ \ol{6}&\ol{7}&\ol{8}\\ \ol{1}&\ol{3}\\}
\]
\end{example}





\subsection{AMBC}
Here we briefly recall some notations from \cite{CPY} and \cite{CLP} and discuss the relations between the affine matrix-ball construction (abbreviated AMBC) and Kazhdan-Lusztig cells. 

For $T=(T_1, \ldots, T_l)\in \RSYT(\lambda)$ and $i\in [1, l-1]$ we define $\lch_i(T)$, called the local charge in row $i$ of $T$, as follows. Suppose that $T_i=(a_1, \ldots, a_s)$ and $T_{i+1} = (b_1, \ldots, b_t)$. Then $\lch_i(T)$ is the smallest $d\in \NN$ such that $a_{l-d}<b_{l}$ for $l\in [d+1, t]$. Pictorially, this measures necessarily shift of $T_i$ to the right so that $(T_i, T_{i+1})$ becomes a standard Young tableau (of skew shape). For example, if $T_i = (3,5,7,8)$ and $T_{i+1} = (1,2,4,6)$ then we have $\lch_i(T) = 2$ which can be obtained from the following picture.
\[\ytableaushort{3578,1246} \quad \Rightarrow\quad \ytableaushort{{\none}{\none}3578,1246}
\]


For $P, Q \in \RSYT(\lambda)$ where $\lambda=(\lambda_1, \ldots, \lambda_l)$, we define the symmetrized offset constants with respect to $(P, Q)$, denoted by $\offset(P,Q)=(s_1, \ldots, s_l) \in \ZZ^l$, as follows.
\[s_i = 
\left\{\begin{aligned}
&0& \textnormal{ if } i=1 \textnormal{ or } \lambda_i>\lambda_{i+1},
\\&s_{i-1}+\lch_{i-1}(P)-\lch_{i-1}(Q) & \textnormal{ otherwise}.
\end{aligned}\right.
\]
In other words, we have $s_i-s_{i-1} = \lch_{i-1}(P)-\lch_{i-1}(Q)$ whenever $\lambda_{i-1}=\lambda_i$. (See \cite[Definition 5.8]{CPY} and \cite[Theorem 5.10]{CLP} for the equivalent definitions.) It is easy to show that for tabloids $P, Q, R$ of the same shape, we have
$$\offset(P,Q)+\offset(Q,R) = \offset(P,R), \textup{ thus in particular } \offset(P,Q)+\offset(Q,P)=\offset(P,P)=0.$$




\begin{example} \label{ex:1}
Let $P= \yttab{\ol{2}&\ol{4}&\ol{6} \\ \ol{3}&\ol{7}&\ol{8} \\ \ol{1}&\ol{5}&\ol{9}}$ and
$Q= \yttab{\ol{3}&\ol{5}&\ol{7} \\ \ol{1}&\ol{2}&\ol{8} \\ \ol{4}&\ol{6}&\ol{9}}$. 
Then $\lch_1(P) = 0, \lch_2(P) = 1, \lch_1(Q)=2$, and $\lch_2(Q)=0$. Thus it follows that $\offset(P,Q) = (0,-2,-1)$.  
\end{example}

For $\lambda=(\lambda_1, \ldots, \lambda_r)$ and $\boldsymbol{\rho} = (\rho_1, \ldots, \rho_r)$, we define $\rev_\lambda(\boldsymbol{\rho})$ 
to be the integer vector obtained from $\boldsymbol{\rho}$ by reversing the order of elements corresponding to the parts of the same length in $\lambda$. For example, if $\lambda=(2,2,1,1,1)$ and $\boldsymbol{\rho}=(3,1,5,2,4)$ then we have $\rev_\lambda(\boldsymbol{\rho}) = (1,3,4,2,5)$. We say that $\boldsymbol{\rho} \in \ZZ^{l(\lambda)}$ is \defn{dominant} with respect to $(P,Q)$ if $-(\boldsymbol{\rho}-\offset(P,Q))$ is non-increasing, or equivalently $\rev_\lambda(\boldsymbol{\rho}-\offset(P,Q))$ is non-decreasing.

\begin{example} \label{ex:2}
 In the Example \ref{ex:1} the vector $\boldsymbol{\rho} = (2,0,2) \in \ZZ^3$ is dominant with respect to $(P,Q)$ because $\boldsymbol{\rho} - \offset(P,Q) = (2,2,3)$ is a nondecreasing sequence.
\end{example}


We set
\begin{align*}
\Omega &\colonequals \bigsqcup_{\lambda\vdash n} \RSYT(\lambda) \times \RSYT(\lambda) \times \ZZ^{l(\lambda)},\\
\dom &\colonequals \left\{(P, Q, \boldsymbol{\rho}) \in \Omega \mid \boldsymbol{\rho} \textup{ is dominant with respect to } \right.\\
&\left.(P,Q) \textup{ with  }\sum_i \rho_i = 0 \text{ for }\boldsymbol{\rho}=(\rho_1\cdots,\rho_d)\right\}.
\end{align*}
We define $\Phi \colon \tS_n \rightarrow \dom$ by $w \mapsto (P(w), Q(w), \boldsymbol{\rho}(w))$ to be the bijection defined in \cite{CPY} using the affine matrix-ball construction. Also, let $\Psi \colon \Omega \rightarrow \tS_n$ be the surjection defined by the backward AMBC. Then by \cite[Theorem 5.12]{CPY} we have $\Psi|_{\dom} = \Phi^{-1}$. Both AMBC and backward AMBC were explained in great detail in both \cite{CPY} and \cite{CLP}.


For the left and right descent set $L(w)$ and $R(w)$ for $w$, we have 
\begin{proposition}
\label{prop:descents}
For any permutation $w$, $L(w) = \tau(P(w))$ and $R(w) = \tau(Q(w))$.
\end{proposition}



\subsection{Affine $\fpf$ graphs and molecules}
The following definitions are from Stembridge's papers \cite{Stem1,Stem2}.
\begin{definition}
For an algebra $\cA$, an \defn{$I$-labeled graph} for a finite set $I$ is a triple $\Gamma=(V,\omega,\nu)$ where
\begin{itemize}
\item[(i)] $V$ is a finite vertex set;
\item[(ii)] $\omega: V\times V\to \cA$ is a map;
\item[(iii)] $\nu: V\to \mathcal{P}(I)$ is a map assigning a subset of $I$ to each vertex.
\end{itemize}
We view $\Gamma$ as a weighted directed graph on the vertex set $V$ with an edge  $x\xrightarrow{\omega(x,y)} y$ if $\omega(x,y)\neq 0$.
\end{definition}

Now assume that $(W,S)$ is a Coxeter system and $\cH$ is the corresponding Iwahori-Hecke algebra. 
\begin{definition}
An $S$-labeled graph $\Gamma=(V,\omega,\nu)$ is a \defn{$W$-graph} if the free $\cA$-module generated by $V$ can be given an $\cH$-module structure with
\[
H_sx=\begin{cases}vx&s\not\in\nu(x)\\
-v^{-1}x+\sum_{y\in V;s\not\in\nu(y)}\omega(x,y)y&s\in\nu(x)\end{cases}
\qquad \text{ for }s\in S\text{ and }x\in V.
\]
\end{definition}

\begin{example}
There exist a unique ring homomorphism $\cH\to\cH$ with $v\mapsto v^{-1}$ and $H_s\mapsto H_s^{-1}$; we denote this map by $H\mapsto\overline{H}$, and refer to it as the \defn{bar operator} of $\mathcal{H}$.
Write $<$ for the Bruhat order on $W$.
By well-known results of Kazhdan and Lusztig \cite{KL},
for each $w\in W$ there is a unique $\underline H_w\in\mathcal{H}$ with
$$\underline{H}_w = \overline{\underline{H}}_w\in H_w+\sum_{y<w}v^{-1}\ZZ[v^{-1}]H_y.$$
The elements $\{\underline{H}_w\}_{w\in W}$ form an $\cA$-basis for $\mathcal{H}$, called the \defn{Kazhdan-Lusztig basis}.
Define $h_{x,y}\in\ZZ[v^{-1}]$ for $x,y \in W$ such that $\underline{H}_y=\sum_{x\in W}h_{x,y}H_x$,
%Then $h_{x,y}=0$ if $x\nless y$ and $h_{1,s}=1$ for $s\in S$. Define 
and let $\mu(x,y)$ be the coefficient of $v^{-1}$ in $h_{x,y}$. 
Finally, let 
$$ \nu(x) = \{ s \in S : \ell(sx) < \ell(x)\}\qquand \omega(x,y)=\begin{cases}\mu(x,y)&\text{if }\nu(x)\not\subset\nu(y)\\0,&\text{otherwise}\end{cases}$$
for $x,y \in W$.
Then $\Gamma = (W,\omega,\nu)$ is a $W$-graph \cite{KL}.
\end{example}

We turn back to the case $W=\tS_n$. Let $\cM =\cA\text{-span}\{M_z:z\in\cF_n\}$ and $
\cN=\cA\text{-span}\{N_z:z\in\cF_n\}$
denote the free $\cA$-modules with bases given by the symbols $M_z$ and $N_z$ for $z\in\cF_n$. We call $\{M_z\}_{z\in\cF_n}$ and $\{N_z\}_{z\in\cF_n}$ the standard bases of $\cM$ and $\cN$, respectively. 
Let $S = \{s_1,s_2,\dots,s_n\}$.

\begin{proposition}[{\cite[Corollary 4.15]{Zhang}}]
Both $\cM$ and $\cN$ have unique $\cH$-module structures such that if $s\in  S$ and $z\in\cF_n$ then we have 
{\small\[H_sM_z=\begin{cases}
M_{szs}&\ellfpf(szs)>\ellfpf(z)\\
vM_{z}&\ellfpf(szs)=\ellfpf(z)\\
M_{szs}+(v-v^{-1})M_z&\ellfpf(szs)<\ellfpf(z)\end{cases}
\]}
and
{\small\[H_sN_z=\begin{cases}
N_{szs}&\ellfpf(szs)>\ellfpf(z)\\
-v^{-1}N_{z}&\ellfpf(szs)=\ellfpf(z)\\
N_{szs}+(v-v^{-1})N_z&\ellfpf(szs)<\ellfpf(z).\end{cases}
\]}
\end{proposition}

Rains and Vazirani's general theory of quasiparabolic sets gives us for free the $\mathcal{H}$-module structures described in the previous result. These modules are potentially interesting to study on their own. We note a few special properties which follow from results in \cite{Marberg}.

We write $f \mapsto \overline{f}$ for the automorphism of $\cA$ interchanging $v$ and $v^{-1}$. A map $U\to V$ of $\cA$-modules is \defn{$\cA$-antilinear} if $x\mapsto y$ implies $ax\mapsto\overline{a}y$ for all $a\in\cA$.


\begin{corollary}[{\cite[Corollary 4.16]{Zhang}}]
The $\cH$-modules $\cM$ and $\cN$ have the following properties:
\begin{itemize}
\item[(a)] There are unique $\cA$-antilinear maps $\cM\to\cM$ and $\cN\to\cN$, which we write as $X \mapsto\overline X$,
with 
$$\overline{HM}=\overline{H}\cdot\overline{M}\text{ and }\overline{M_{\Theta}}=M_{\Theta}
\qquand
\overline{HN}=\overline{H}\cdot\overline{N}\text{ and }\overline{N_{\Theta}}=N_{\Theta}
$$
for all $M \in \cM$, $N \in \cN$, and $\Theta \in \{\Theta^\pm\}$. Moreover, both of these maps are involutions.

\item[(b)] The $\mathcal{H}$-modules
$\cM$ and $\cN$ have unique $\cA$-bases $\{\underline{M}_x\}_{x \in \cF_n}$ and $\{\underline{N}_x\}_{x \in \cF_n}$ satisfying
$$\underline{M}_x=\overline{\underline{M}_x}\in M_x+\sum_{w<_F x}v^{-1}\ZZ[v^{-1}]M_w
\qquand
\underline{N}_x=\overline{\underline{N}_x}\in N_x+\sum_{w<_F x}v^{-1}\ZZ[v^{-1}]N_w$$
where both sums are over $w\in\cF_n$. We refer to these as  the \defn{canonical bases} of $\cM$ and $\cN$.

\end{itemize}
\end{corollary}

Define $\m_{x,y}$ and $\n_{x,y}$ for $x,y\in \cF_n$ as the polynomials in $\ZZ[v^{-1}]$ such that
$$\underline{M}_y=\sum_{x\in  \tS_n}\m_{x,y}M_x\qquand \underline{N}_y=\sum_{x\in \tS_n}\n_{x,y}N_x.$$
Let $\mu_\m(x,y)$ and $\mu_\n(x,y)$ denote the coefficients of $v^{-1}$ in $\m_{x,y}$ and $\n_{x,y}$.
Define $\nu_\m$, $\nu_\n$: $\cF_n\to \mathcal{P}(S)$ by
\[
\nu_\m(x) = \{s\in S : sxs \le_F x\}\qquand \nu_\n(x) = \{s\in S : x\le_F sxs\}
\]
where $S = \{s_1,s_2,\dots,s_n \} \subset \tS_n$.
Finally, let $\omega_\m$: $\cF_n\times\cF_n\to\ZZ$ be the map with
\[
\omega_\m(x,y)=\begin{cases}\mu_\m(x,y)+\mu_\m(y,x)&\nu_\m(x)\not\subset\nu_\m(y)\\
0&\nu_\m(x)\subset\nu_\m(y).\end{cases}
\]
Define $\omega_\n$: $\cF_n\times\cF_n\to\ZZ$ by the same formula, but with $\mu_\m$ and $\nu_\m$ replaced by $\mu_\n$ and $\nu_\n$.

\begin{corollary}[{\cite[Theorem 3.26]{Marberg}}]
Both $\Gamma^\m_n=(\cF_n,\omega_\m,\nu_\m)$ and $\Gamma^\n_n=(\cF_n,\omega_\n,\nu_\n)$ are $\tS_n$-graphs.
\end{corollary}
We call these graphs \defn{affine $\fpf$ graphs}. The strongly connected components in a $W$-graph $\Gamma$ are called \defn{cells}. The connected components with respect to bidirected edges are called \defn{molecules}. It is generally an interesting problem to determine the cells and molecules in a given $W$-graph.

If $n=2$ then one can show that
$\Gamma^\m_n$ and $\Gamma^\n_n$ both decompose into just two cells given by $\cFp$ and $\cFm$. Moreover, each cell consists of one molecule.




\section{Affine Knuth moves and involutive transformation}\label{Knuth-sect}

\begin{definition}
Let $i\in\ZZ$. Two affine permutations $w$ and $w'$ are connected by a \defn{Knuth move} at position $\widebar i$ if all of the following hold:
\begin{itemize}
\item for all $j$ such that $j\equiv i\pmod{n}$, we have $w'(j)=w(j+1)$ and $w'(j+1)=w(j)$;
\item for all $j$ such that $j\not\equiv i\pmod{n}$, $j\not\equiv i+1\pmod{n}$, we have $w'(j)=w(j)$;
\item at least one of $w(i+2)$ and $w(i-1)$ is numerically between $w(i)$ and $w(i+1)$.
\end{itemize}
\end{definition}


We denote it as $w\K{\widebar i}w'$. Also, we write $w\K{\widebar i}w$ if $w(i+2)$ and $w(i-1)$ are not numerically between $w(i)$ and $w(i+1)$.


\begin{definition}
Let $n\ge3$. For a tabloid $T$ and each integer $1\le i\le n$, the \defn{dual equivalence operator} $D_i$ is the map acting on $T$ by
\[
D_i(T) :=\begin{cases}
s_{i}(T) &\text{ if }\widebar i\in\tau(T)\setminus\tau(s_i(T)), \overline{i\pm1}\in\tau(s_i(T))\setminus\tau(T)\\
&\text{ or }\overline{i\pm1}\in\tau(T)\setminus\tau(s_i(T)), \widebar i\in\tau(s_i(T))\setminus\tau(T)\\
T &\text{ otherwise.}
\end{cases}
\]
Here $i\pm1$ means one of $i+1$ and $i-1$.
\end{definition}
For $x,y\in[n]$, we say $x\prec_T y$ if $y$ appears above $x$ and $x\preceq_T y$ if $y$ appears above $x$ or in the same row as $x$.
Then we have $D_i(T)=s_{i}(T)$ if and only if one of the following happens:
$i+1\preceq_Ti+2\prec_Ti$, $i\preceq_Ti+2\prec_Ti+1$, 
$i+1\prec_Ti-1\preceq_Ti$, $i\prec_Ti-1\preceq_Ti+1$.


\begin{definition}
Let $T=(T_1,T_2,\cdots)$ be a tabloid of shape $\lambda$ and suppose that $i\in T_t$ for some $1\le i\le n$ and $1\le t\le l=l(\lambda)$. We define
$\delta(T,i)=(\delta_1,\delta_2,\cdots,\delta_l)$ and $\iota(T,i)=(\iota_1,\iota_2,\cdots,\iota_l)$ as follows. Here $[-]$ is the Iverson bracket.
\begin{itemize}
\item If $\lambda_t=\lambda_{t-1}$, then we put $\delta(T,i)=0$. Otherwise,
$\delta(T,i)=([\lambda_j =\lambda_t])_{1\le j\le l}$.
\item $\iota(T, i)=([j = t])_{1\le j\le l}=([i\in T_j ])_{1\le j\le l}$.
\end{itemize}
\end{definition}

\begin{theorem}[{\cite[Theorem 3.11]{CLP}}]\label{Kthm}
Suppose $w$ is an affine permutation with $\Phi(w)=(P,Q,\rho)$ and $w'\K{\widebar i}w$. Then $\Phi(w')=(P,D_i(Q),\rho+\iota(Q,1)-\iota(Q,n))$.
\end{theorem}

\begin{definition}
Let $i\in\ZZ$. Two affine permutations $w$ and $w'$ are connected by a \defn{dual Knuth move} at position $\widebar i$ if $w^{-1}$ and $(w')^{-1}$ are connected by a Knuth move at position $\widebar i$.
\end{definition}
We denote it as $w\dK{\widebar i}w'$. We also write $w\dK{\widebar i}w$ if $w\K{\widebar i}w$.


\begin{proposition}[{\cite[Proposition 5.10]{KP}}]\label{dKprop}
Suppose $w$ is an affine permutation with $\Phi(w)=(P,Q,\rho)$ and $w'\dK{\widebar i}w$. Then $\Phi(w')=(D_i(P),Q,\rho-\iota(P,1)+\iota(P,n))$.
\end{proposition}

\begin{definition}
Let $i\in\ZZ$. Two affine involutions $z$ and $z'$ are connected by an \defn{involutive transformation} at position $\widebar i$ if there exists an affine permutation $w$ such that $z\K{\widebar i}w\dK{\widebar i}z'$.
\end{definition}
We denote it as $w\simRBS{\widebar i}w'$.

\begin{corollary}[{\cite[Proposition 5.10]{KP}}]\label{invol-cor}
Suppose $w$ is an affine involution with $\Phi(w)=(P,P,\rho)$. Then  $w'\simRBS{\widebar i}w$ if and only if $\Phi(w')=(D_i(P),D_i(P),\rho)$.
\end{corollary}


\begin{theorem}\label{direct-prop} 
If $x,y\in \tilde{I}_n$ have $x\simRBS{\widebar{i}}y$   and 
$A:=\{i-1,i,i+1\}$ then
\[
y = \begin{cases} x &\text{if $x(A) \neq A$ and $x(i)$ is between $x(i-1)$ and $x(i+1)$}
\\
 (i-1,i)x (i-1,i) &\text{if $x(A) \neq A$ and $x(i+1)$ is between $x(i-1)$ and $x(i)$}
\\
(i,i+1)x (i,i+1) &\text{if $x(A) \neq A$ and $x(i-1)$ is between $x(i)$ and $x(i+1)$ }
\\
 (i-1,i+1)x(i-1,i+1) &\text{if $x(A) = A$}.
\end{cases}
\]
\end{theorem}
\begin{proof}
First suppose $x(A)=A$. Then $x$ can restrict to one of four involutions of $A$.
 If $x|_A$ is the identity permutation or $(i-1,i+1)$, then the sequence $x(i-1)x(i)x(i+1)$ is strictly increasing or decreasing
 so $x \K{\widebar i} x =x^{-1} \dK{\widebar i} x^{-1} = x$, but we also have $x=(i-1,i+1)x(i-1,i+1)$, so 
  $x\simRBS{\widebar i} (i-1,i+1)x(i-1,i+1)$ as claimed.
 If $x|_A$ is the adjacent transposition $(i-1,i)$ then the sequence $x(i-1)x(i)x(i+1)$ is $i(i-1)(i+1)$
 so 
 \[ x\K{\widebar i} x(i,i+1)  \dK{\widebar i} (i-1,i) x(i,i+1)  = (i-1,i+1)x(i-1,i+1) \]
 as claimed.
 Similarly, if $x|_A$ is $(i,i+1)$ then  $x(i-1)x(i)x(i+1)$ is  $(i-1)(i+1)i$ so 
 \[ x\K{\widebar i}x(i-1,i)\dK{\widebar i}(i,i+1)x(i-1,i)= (i-1,i+1)x(i-1,i+1) \]
 as claimed.

For the rest of this proof we assume   $x(A)\neq A$. Define $a<b<c$ to be the numbers with $\{a,b,c\} = \{x(i-1),x(i),x(i+1)\}$.
If $x(i)$ is between $x(i-1)$ and $x(i+1)$, then the sequence $x(i-1)x(i)x(i+1)$ is either increasing or decreasing so $x\K{\widebar i}x= x^{-1} \dK{\widebar i} x^{-1} =x$ and $x\simRBS{\widebar i} x$ as claimed.

Suppose instead that $x(i+1)$ is between $x(i-1)$ and $x(i)$.
Then $x(i-1)x(i)x(i+1)$ is either $cab$ or $acb$ so $x\K{\widebar i}xs_{i-1}$ and $x\dK{\widebar i} s_{i-1} x$.
The relative order of $i-1$, $i$, and $i+1$ in the one-line notation of $xs_{i-1}$
 can only differ from that of $x$ if $\{x(i-1),x(i)\} = \{a,c\}\equiv B\pmod{n}$ for a subset $B\subset \{i-1,i,i+1\}$. Then if $B=\{a,c\}$, we must have $x(A) = \{a<b<c\} = \{i-1,i,i+1\}=A$.
As we assume $x(A) \neq A$, 
the relation $x\dK{\widebar i} s_{i-1} x$ implies that $xs_{i-1} \dK{\widebar i} s_{i-1} x s_{i-1}$,
so $x\simRBS{\widebar i} s_{i-1}x s_{i-1}$ as claimed.
If $B\neq\{a,c\}$, then we have $\{a<c\}=\{i-1-kn,i+kn\}$ or $\{i-kn,i-1+kn\}$ for some positive integer $k$. Then $s_{i-1}x(i-1),s_{i-1}x(i),s_{i-1}x(i+1)$ is $x(i-1)\pm1,x(i)\mp1,x(i+1)$, which is either $cab$ or $acb$ so $s_{i-1}x\K{\widebar i}s_{i-1}xs_{i-1}$ and $xs_{i-1}\dK{\widebar i}s_{i-1}xs_{i-1}$. Therefore, in both cases, we have $x\simRBS{\widebar i}s_{i-1}xs_{i-1}$.

Finally, suppose 
 $x(i-1)$ is between $x(i)$ and $x(i+1)$. Then $x(i-1)x(i)x(i+1)$ is either $bca$ or $bac$ so $x\K{\widebar i}xs_{i}$
 and $x\dK{\widebar i} s_{i}x$. 
Now the relative order of $i-1$, $i$, and $i+1$ in the one-line notation of $xs_{i}$
 can only differ from that of $x$ if $\{x(i),x(i+1)\} = \{a,c\}\equiv B\pmod{n}$ for a subset $B\subset \{i-1,i,i+1\}$. Then if $B=\{a,c\}$, we must again have $x(A)=A$. Therefore $x\dK{\widebar i} s_{i} x$ implies that $xs_{i} \dK{\widebar i} s_{i} x s_{i}$ so  $x\simRBS{\widebar i} s_{i}x s_{i}$.
If $B\neq\{a,c\}$, then we have $\{a<c\}=\{i-kn,i+1+kn\}$ or $\{i+kn,i+1-kn\}$ for some positive integer $k$. Then $s_ix(i-1),s_ix(i),s_ix(i+1)$ is $x(i-1),x(i)\mp1,x(i+1)\pm1$, which is either $cab$ or $acb$ so $s_ix\K{\widebar i}s_ixs_i$ and $xs_i\dK{\widebar i}s_ixs_i$. Therefore, in both cases, we have $x\simRBS{\widebar i} s_{i}x s_{i}$.
\end{proof}

\section{Shift and Knuth moves}\label{shift-sect}
Define $\omega=[2,\cdots,n,n+1]$. Then we have the following proposition.

\begin{proposition}[{\cite[Proposition 5.5]{KP}}]\label{omega_prop}
Suppose $w$ is an affine permutation with $\Phi(w)=(P,Q,\rho)$. Then 
\begin{align*}
\Phi(\omega w)&=(\omega(P),Q,\rho-\bold{s}(P,Q)+\bold{s}(\omega(P),Q)+\delta(Q,n)),\\
\Phi(w\omega^{-1})&=(P,\omega(Q),\rho-\bold{s}(P,Q)+\bold{s}(P,\omega(Q))-\delta(Q,n)).
\end{align*}
Moreover, if $w$ is an involution, then $\Phi(\omega w\omega^{-1})=(\omega(P),\omega(P),\rho)$.
\end{proposition}
Note that we have $\omega(P)=s_1s_2\cdots s_{n-1}P$

\begin{proposition}\label{omega-it-prop}
Suppose $x,y$ are affine involutions, then $x\simRBS{\widebar{i}}y$ if and only if $\omega x\omega^{-1} \simRBS{\widebar{i+1}}\omega y\omega^{-1}$.
\end{proposition}
\begin{proof}
First suppose $x\simRBS{\widebar{i}}y$ and denote $A=\{i-1,i,i+1\}$, $\omega x\omega^{-1}=x'$, $\omega x'\omega^{-1}=y'$, then we have 
\[
y = \begin{cases} x &\text{if $x(A) \neq A$ and $x(i)$ is between $x(i-1)$ and $x(i+1)$}
\\
 (i-1,i)x (i-1,i) &\text{if $x(A) \neq A$ and $x(i+1)$ is between $x(i-1)$ and $x(i)$}
\\
(i,i+1)x (i,i+1) &\text{if $x(A) \neq A$ and $x(i-1)$ is between $x(i)$ and $x(i+1)$ }
\\
 (i-1,i+1)x(i-1,i+1) &\text{if $x(A) = A$}.
\end{cases}
\]
Since $x'(j)=x(j-1)+1$, then $x(A)=A$ if and only if $x'(A+1)=A+1$. So it is easy to see
\[
y'= \begin{cases} x'&\text{if $x'(A+1) \neq A+1$, $x'(i+1)$ is between $x'(i)$ and $x'(i+2)$}
\\
 (i-1,i)x'(i-1,i) &\text{if $x'(A+1) \neq A+1$, $x'(i+2)$ is between $x'(i)$ and $x'(i+1)$}
\\
(i,i+1)x'(i,i+1) &\text{if $x'(A+1) \neq A+1$, $x'(i)$ is between $x'(i+1)$ and $x'(i+2)$ }
\\
 (i-1,i+1)x'(i-1,i+1) &\text{if $x'(A+1) = A+1$}.
\end{cases}
\]
Therefore $x' \simRBS{\widebar{i+1}}y'$. The converse can be proved similarly.
\end{proof}

\begin{proposition}\label{omega-prop}
If $w$ is an affine fixed-point-free involution, then $w$ and $\omega w\omega^{-1}$ cannot be connected by a sequence of involutive transformations.
\end{proposition}
\begin{proof}
Suppose $w$ is in the conjugacy class of $\Theta^\pm$, then $\omega w\omega^{-1}$ is in the conjugacy class of $\Theta^\mp$. This is because we have $\omega\Theta^+\omega^{-1}=\Theta^-$. Then $w$ and $\omega w\omega^{-1}$ cannot be connected by a sequence of involutive transformations.
\end{proof}



\begin{theorem}\label{omega-thm}
Suppose $w$ is an affine fixed-point-free involution with $\Phi(w)=(P,P,\rho)$. For each $P'$ of the same shape as $P$, let $w'=\Psi(P',P',\rho)$. Then there exists $i\in[n]$ such that $w$ and $\omega^i w'\omega^{-i}$ are connected by a sequence of involutive transformations.
\end{theorem}
\begin{proof}
Let $\mathcal A_\lambda$ be the graph whose vertices are the
tabloids of shape $\lambda$, with an $i$-edge between $T$ and
$D_i(T)$ whenever $D_i(T)\neq T$.

For $a\in \mathbb Z/n\mathbb Z$, let $C_a$ be the column
superstandard tabloid of shape $\lambda$ with start $a$.
By \cite[Lemma 7.4]{CLP}, every connected component of
$\mathcal A_\lambda$ contains some $C_a$.

The shift $\omega$ is an automorphism of $\mathcal A_\lambda$,
since it cyclically permutes the dual equivalence operators, and
it satisfies
\[
\omega(C_a)=C_{a+1}.
\]
Choose $a,b\in\mathbb Z/n\mathbb Z$ such that 
\[
P\sim_D C_a
\qquad\text{and}\qquad
P'\sim_D C_b.
\]
Choose $i$ such that $b+i\equiv a\pmod n$. Applying $\omega^i$
to a path from $P'$ to $C_b$ gives
\[
\omega^i(P')\sim_D C_{b+i}=C_a.
\]
Hence
\[
P\sim_D \omega^i(P').
\]

Write this path as
\[
P=T_0\sim_D T_1\sim_D\cdots\sim_D T_r=\omega^i(P'),
\]
where $T_{k+1}=D_{j_k}(T_k)$. Since $\offset(T_k,T_k)=0$ for every $k$, dominance of $\rho$
with respect to $(T_k,T_k)$ depends only on $\lambda$ and
$\rho$. Hence $(T_k,T_k,\rho)\in\Omega_{\mathrm{dom}}$.

Define
\[
z_k=\Psi(T_k,T_k,\rho).
\]
Then $\Phi(z_k)=(T_k,T_k,\rho)$, and Corollary~\ref{invol-cor} implies
\[
z_k\simRBS{\widebar j_k}z_{k+1}.
\]
Starting with $z_0=w$, all the $z_k$ are affine
fixed-point-free involutions, since an involutive transformation
is either trivial or is given by conjugation, as in
Theorem~\ref{direct-prop} .

Finally,
\[
\Phi(z_r)=(\omega^i(P'),\omega^i(P'),\rho).
\]
By Proposition~\ref{omega_prop},
\[
\Phi(\omega^{-i}z_r\omega^i)=(P',P',\rho).
\]
Since $\Phi$ is injective on $\Omega_{\mathrm{dom}}$, it follows
that
\[
\omega^{-i}z_r\omega^i
=\Psi(P',P',\rho)=w'.
\]
Thus $z_r=\omega^i w'\omega^{-i}$, and hence $w$ and
$\omega^i w'\omega^{-i}$ are connected by a sequence of
involutive transformations.
\end{proof}

In the following sections, we work with affine fixed-point-free involutions. In this case, we define 
\[
\Pafpf(n)
:=
\left\{
\lambda \vdash n \ \middle|\
\begin{array}{c}
\text{there exists } z\in\mathcal{F}_n
\text{ such that}\\
\Phi(z)=(P,P,\rho)
\text{ for some }P\in\operatorname{RSYT}(\lambda)
\text{ and }\rho\in\mathbb{Z}^{\ell(\lambda)}
\end{array}
\right\}.
\]

\section{An asymptotic row-Beissinger realization of AMBC}\label{sec:affine-rb}

In this section we give a finite approximation of the AMBC data of an
affine fixed-point-free involution using row Beissinger insertion.  The
construction has three parts.  First, we truncate the periodic matching of
an element of $\cF_n$ by taking complete two-cycles.  Second, we show that
the rows of the resulting finite row Beissinger tableaux stabilize and
recover the AMBC tabloid.  Finally, we record the eventual horizontal
positions of the stable rows and show that their blockwise normalization is
the AMBC weight.

Throughout this section, let $n$ be even and let $x\in\cF_n$.  We write
\[
\Phi(x)=(P,P,\rho),
\qquad
\lambda=\operatorname{shape}(P)
      =(\lambda_1,\ldots,\lambda_{\ell}).
\]

For a tableau or tabloid $T$ and an entry $a$ of $T$, we write $\row_T(a)$ for the index of the row containing $a$, where rows are numbered
from top to bottom starting with $1$.  If $T$ is a tableau, we similarly write $\col_T(a)$ for the index of the column containing $a$, where columns are
numbered from left to right starting with $1$.

\subsection{Complete two-cycle truncations}

We first recall the finite row Beissinger correspondence.  If $T$ is a
partially standard tableau and $a\leq b$, let
\[
T\xleftarrow{\rB}(a,b)
\]
be the tableau obtained as follows.  If $a<b$, Schensted row-insert $a$
into $T$; if the new box lies in row $r$, then append $b$ to row $r+1$.
If $a=b$, append $a$ to the first row.  Given a finite involution $z$, list
its pairs
\[
(a_1,b_1),\ldots,(a_q,b_q),
\qquad
 a_s\leq b_s=z(a_s),
\qquad
 b_1<\cdots<b_q,
\]
and define
\[
\PrB(z)
=
\varnothing
\xleftarrow{\rB}(a_1,b_1)
\xleftarrow{\rB}\cdots
\xleftarrow{\rB}(a_q,b_q).
\]

\begin{theorem}[{\cite[Theorem 3.1]{Beissinger}}]
\label{finite-rb-thm}
If $z$ is a finite involution, then
\[
\PrB(z)=\PRSK(z)=\QRSK(z).
\]
\end{theorem}

The two-cycles of $x$ form an $n$-periodic perfect matching on $\ZZ$.
Translation by $n$ acts freely on the set of these two-cycles.  Each
translation orbit has a unique representative $(a,b)$ satisfying
\[
a<b=x(a)
\qquad\text{and}\qquad
1\leq b\leq n.
\]
Since $x$ is fixed-point-free, there are exactly $n/2$ such orbits.

\begin{definition}[Complete two-cycle truncation]
\label{complete-cycle-def}
Let
\[
\cE(x)=\{(a_1,b_1),\ldots,(a_{n/2},b_{n/2})\}
\]
be the representatives above, indexed so that
\[
b_1<b_2<\cdots<b_{n/2}.
\]
For $N\in\NN$, set
\[
\cE_N(x)
=
\left\{
(a_s+kn,b_s+kn):
1\leq s\leq \frac n2,\ -N\leq k\leq N
\right\}
\]
and
\[
S_N(x)=
\bigcup_{(a,b)\in\cE_N(x)}\{a,b\}.
\]
The set $\cE_N(x)$ defines a finite fixed-point-free involution
$x_N$ on $S_N(x)$ by
\[
x_N(a)=b
\qquad\text{and}\qquad
x_N(b)=a
\qquad ((a,b)\in\cE_N(x)).
\]
Let
\[
\sigma_N:S_N(x)\longrightarrow[(2N+1)n]
\]
be the unique order-preserving bijection and put
\[
\widetilde{x}_N
=
\sigma_N\circ x_N\circ\sigma_N^{-1}.
\]
The tableau
\[
\BrB_N(x)
:=
\sigma_N^{-1}\bigl(\PrB(\widetilde{x}_N)\bigr),
\]
where $\sigma_N^{-1}$ is applied entrywise, is called the
\defn{$N$-th complete two-cycle row Beissinger truncation} of $x$.
\end{definition}

\begin{proposition}
\label{rb-rsk-trunc-prop}
For every $N\in\NN$, one has
\[
\BrB_N(x)
=
\sigma_N^{-1}\bigl(\PRSK(\widetilde{x}_N)\bigr)
=
\sigma_N^{-1}\bigl(\QRSK(\widetilde{x}_N)\bigr).
\]
\end{proposition}

\begin{proof}
The permutation $\widetilde{x}_N$ is a finite involution, so the result
follows immediately from Theorem~\ref{finite-rb-thm}.
\end{proof}

\subsection{Stable PNAP sequences}

We use the stabilization framework of \cite[Section~12]{CPY}.  A
\defn{partial non-affine permutation}, abbreviated PNAP, is a finite
sequence of distinct integers and empty symbols.  It may be represented by
its balls in a finite matrix.  A sequence $(f_k)_{k\geq0}$ of PNAPs is
\defn{stable} if, for all sufficiently large $k$, the balls added in
passing from $f_k$ to $f_{k+1}$ are the $(n,n)$-translates of the balls
added in passing from $f_{k-1}$ to $f_k$.  It is \defn{uniformly bounded}
if the diagonals containing its balls lie in a fixed bounded interval.

Two sequences of PNAPs of the same lengths are \defn{asymptotically alike}
if they agree away from initial and terminal intervals of uniformly
bounded length.  Finally, a sequence $(f_k)$ \defn{$P$-stabilizes in row
$r$ to $A\subseteq[\ol n]$} if the multiset of residues in row $r$ of
$\PRSK(f_k)$ grows eventually by precisely the set $A$ at every step.
We use the following results.

\begin{proposition}[PNAP stabilization]
\label{pnap-stability-prop}
The following statements hold.
\begin{enumerate}
\item Every uniformly bounded stable sequence of PNAPs $P$-stabilizes in
      every row.
\item If two uniformly bounded sequences are asymptotically alike and
      $P$-stabilize in a fixed row, then their stabilizing subsets of
      $[\ol n]$ in that row are equal.
\item For an affine permutation $w$, the one-sided truncations
      \[
      (w(1),w(2),\ldots,w(kn))
      \]
      $P$-stabilize to the AMBC insertion tabloid $P(w)$.
\end{enumerate}
\end{proposition}

\begin{proof}
These are \cite[Lemma~12.4, Corollary~12.9, and
Theorem~7.3]{CPY}, respectively.
\end{proof}

For later use, define the displacement bound
\[
D_x:=\max_{1\leq i\leq n}|x(i)-i|.
\]
Periodicity implies that $|x(j)-j|\leq D_x$ for every $j\in\ZZ$.

Fix $j\in[n]$.  Translate every label in the $N$-th complete-cycle
truncation by $Nn$, and denote the resulting finite involution by $y_N$.
Thus the selected cycles of $y_N$ are indexed by the consecutive periods
$0,1,\ldots,2N$.  Let $g_N^{(j)}$ be the restriction of $y_N$ to domain
labels at most $j+Nn$, regarded as a PNAP after inserting empty symbols at
missing domain positions, and put
\[
h_N^{(j)}:=\bigl(g_N^{(j)}\bigr)^{-1}.
\]

\begin{lemma}[Comparison with one-sided truncations]
\label{complete-cycle-pnap-lem}
For each $j\in[n]$, the sequence
\[
\bigl(h_N^{(j)}\bigr)_{N\geq0}
\]
is uniformly bounded and stable.  After adding a uniformly bounded number
of empty symbols at its two ends, it is asymptotically alike to the usual
one-sided truncations of $x^{-1}$.
\end{lemma}

\begin{proof}
Uniform boundedness follows from
\[
|x(a)-a|\leq D_x.
\]
When $N$ is increased by one, the upper bound on the domain of
$g_N^{(j)}$ changes from $j+Nn$ to $j+(N+1)n$.  Away from a bounded
neighborhood of the lower endpoint, the new balls are exactly the
$(n,n)$-translates of the balls added at the previous step.  Hence
$(g_N^{(j)})_N$ is stable.  Reflection in the main diagonal commutes with
translation by $(n,n)$, so the inverse sequence $(h_N^{(j)})_N$ is stable
as well.

The balls of $h_N^{(j)}$ and those of a one-sided truncation of $x^{-1}$
coincide whenever both coordinates are farther than $D_x$ from the two
boundary lines.  Consequently, after padding by at most a constant number
of empty symbols, independent of $N$, the two PNAPs agree outside initial
and terminal intervals of bounded length.  Thus the two sequences are
asymptotically alike.
\end{proof}

\subsection{The stable tabloid}

\begin{theorem}[Row stability and recovery of the AMBC tabloid]
\label{rb-ambc-tabloid-thm}
For every $j\in[n]$, one has
\[
\row_{\BrB_N(x)}(j)
=
\row_P(\ol j)
\]
for all sufficiently large $N$.  In particular, the rows of the complete
two-cycle row Beissinger truncations stabilize.
\end{theorem}

\begin{proof}
Fix $j\in[n]$.  Translation of all labels preserves row positions, and
Proposition~\ref{rb-rsk-trunc-prop} gives
\[
\row_{\BrB_N(x)}(j)
=
\row_{\QRSK(y_N)}(j+Nn).
\]
The position of a label in a recording tableau is fixed at the moment the
label is added.  Therefore
\[
\row_{\QRSK(y_N)}(j+Nn)
=
\row_{\QRSK(g_N^{(j)})}(j+Nn).
\]
By symmetry of the Robinson--Schensted correspondence,
\[
\QRSK(g_N^{(j)})
=
\PRSK(h_N^{(j)}).
\]
Hence
\begin{equation}
\label{rb-row-prefix-eq}
\row_{\BrB_N(x)}(j)
=
\row_{\PRSK(h_N^{(j)})}(j+Nn).
\end{equation}

By Lemma~\ref{complete-cycle-pnap-lem} and
Proposition~\ref{pnap-stability-prop}, the sequence
$(h_N^{(j)})_N$ has the same row stabilization as the usual one-sided
truncations of $x^{-1}$.  That stabilization is the AMBC tabloid
$P(x^{-1})$.  Since $x=x^{-1}$ and
\[
\Phi(x)=(P,P,\rho),
\]
we have $P(x^{-1})=P$.  The new period in $h_N^{(j)}$ contains the entry
$j+Nn$, representing the residue $\ol j$.  Therefore the right-hand side
of \eqref{rb-row-prefix-eq} is eventually $\row_P(\ol j)$.
\end{proof}

\begin{definition}[Affine row Beissinger tabloid]
\label{affine-rb-tabloid-def}
For $x\in\cF_n$, define
\[
P^{\mathrm{aff}}_{\rB}(x)_r
:=
\left\{
\ol j\in[\ol n]:
\row_{\BrB_N(x)}(j)=r
\text{ for all sufficiently large }N
\right\}.
\]
The resulting tabloid is called the \defn{affine row Beissinger tabloid}
of $x$.
\end{definition}

\begin{corollary}
\label{affine-rb-tabloid-cor}
If $\Phi(x)=(P,P,\rho)$, then
\[
P^{\mathrm{aff}}_{\rB}(x)=P.
\]
\end{corollary}

\subsection{Horizontal phases}

Write the $r$-th row of $P$ as
\[
P_r=\{p_{r,1}<p_{r,2}<\cdots<p_{r,\lambda_r}\}.
\]
The next statement strengthens row stabilization to linear horizontal
stabilization.

\begin{proposition}[Existence of horizontal phases]
\label{rb-phase-existence-prop}
There is a unique vector
\[
\theta(x)=(\theta_1,\ldots,\theta_{\ell})\in\ZZ^{\ell}
\]
such that, for all sufficiently large $N$,
\begin{equation}
\label{rb-phase-eq}
\col_{\BrB_N(x)}(p_{r,t})
=
N\lambda_r+t+\theta_r
\end{equation}
for every $1\leq r\leq\ell$ and
$1\leq t\leq\lambda_r$.
We call $\theta(x)$ the \defn{raw horizontal phase vector} of $x$.
\end{proposition}

\begin{proof}
By Theorem~\ref{rb-ambc-tabloid-thm}, the row of every entry in the
central period is fixed for all sufficiently large $N$.  Passing from the
$N$-th to the $(N+1)$-st complete-cycle truncation adds one complete
period to the left and one complete period to the right.  In the recording
tableau description used in the proof of
Theorem~\ref{rb-ambc-tabloid-thm}, labels from the new right period occur
after every central label and hence do not affect its position.  The new
left period contributes, after stabilization, precisely the residue set
$P_r$ to row $r$.  All these labels are smaller than the central labels
for $N$ sufficiently large.  Consequently,
\[
\col_{\BrB_{N+1}(x)}(p_{r,t})
-
\col_{\BrB_N(x)}(p_{r,t})
=
\lambda_r.
\]
Thus
\[
\col_{\BrB_N(x)}(p_{r,t})-N\lambda_r
\]
is eventually independent of $N$.

The central entries $p_{r,1},\ldots,p_{r,\lambda_r}$ are consecutive
inside row $r$: any integer strictly between two successive such entries
belongs to the central period and, by definition of $P_r$, lies in another
row, while no translate by $n$ can lie between them.  Hence their columns
are consecutive, so the eventual constants have the form
$t+\theta_r$.  Uniqueness is immediate from \eqref{rb-phase-eq}.
\end{proof}

We next relate the differences of the phases to distances between AMBC
channels.  We use the notions of channel, southwest channel numbering,
and channel distance from \cite[Sections~3.3 and~3.4]{CPY}.  For
$1\leq r<\ell$ with $\lambda_r=\lambda_{r+1}$, apply $r-1$ forward steps
to $x$ and let
\[
C_1^{(r)},C_2^{(r)}
\]
be the two southwest-most channels of $\operatorname{fw}^{\,r-1}(x)$, ordered from
southwest to northeast.  Set
\[
h_r(x):=h\bigl(C_1^{(r)},C_2^{(r)}\bigr).
\]

\begin{lemma}[Bulk channel comparison]
\label{bulk-channel-comparison-lem}
Fix a bounded central region in the matrix of $x$.  At every fixed forward
step, the MBC numbering and forward image of the complete-cycle
truncations agree in this region, for all sufficiently large $N$, with the
corresponding southwest channel numbering and AMBC forward image, up to a
common additive shift of the numbering.
\end{lemma}

\begin{proof}
Choose the southwest channel numbering on the relevant affine forward
image.  In a sufficiently large complete-cycle truncation, every complete
numbering class meeting the fixed central region is contained entirely in
the truncation.  The finite MBC numbering on these classes therefore
satisfies the same monotonicity and continuity conditions as the affine
channel numbering and differs from it only by an additive constant.
Boundary-incomplete numbering classes are confined to uniformly bounded
initial and terminal regions.  This is the same truncation argument used
in \cite[Lemma~12.12]{CPY}.

The finite MBC zig-zags through the central region are consequently the
restrictions of the affine zig-zags.  Their outer corner-posts agree, so
the finite and affine forward images coincide in a slightly smaller
central region.  Repeating this argument proves the statement by induction
on the number of forward steps.
\end{proof}

\begin{lemma}[Phase--distance identity]
\label{phase-distance-lem}
If $\lambda_r=\lambda_{r+1}$, then
\[
\theta_{r+1}-\theta_r=h_r(x).
\]
\end{lemma}

\begin{proof}
Let $d=\lambda_r=\lambda_{r+1}$ and extend the two stable rows periodically
by
\[
p_{s,t+d}=p_{s,t}+n
\qquad(s\in\{r,r+1\},\ t\in\ZZ).
\]
Equation~\eqref{rb-phase-eq} says that the leftmost periodic index visible
in row $s$ is
\[
L_s(N)=1-Nd-\theta_s.
\]
Thus the relative displacement of the two equal-density rows is
\[
L_r(N)-L_{r+1}(N)=\theta_{r+1}-\theta_r.
\]
By Lemma~\ref{bulk-channel-comparison-lem}, this displacement is the
stable difference between the two normalized channel numberings on the
next channel.  By the definition of channel distance, that difference is
$h(C_1^{(r)},C_2^{(r)})=h_r(x)$.
\end{proof}

\subsection{Recovery of the AMBC weight}

Let $I=[a,b]$ be a maximal interval of indices for which
\[
\lambda_a=\lambda_{a+1}=\cdots=\lambda_b.
\]
For a vector $\gamma=(\gamma_1,\ldots,\gamma_{\ell})\in\QQ^{\ell}$,
define its blockwise normalization by
\begin{equation}
\label{block-normalization-eq}
\Norm_{\lambda}(\gamma)_i
:=
\gamma_i-
\frac{1}{b-a+1}\sum_{j=a}^{b}\gamma_j
\qquad(i\in I).
\end{equation}

\begin{lemma}[Phase differences and stream altitudes]
\label{phase-altitude-lem}
If $\lambda_r=\lambda_{r+1}$, then
\[
\theta_{r+1}-\theta_r
=
\rho_{r+1}-\rho_r.
\]
\end{lemma}

\begin{proof}
By Lemma~\ref{phase-distance-lem}, the left-hand side is $h_r(x)$.
The AMBC weight coordinates are the altitudes of the streams removed at
successive forward steps.  The channel-distance formula
\cite[Theorem~15.3]{CPY} expresses the change of altitude between
successive equal-density streams as the distance between the corresponding
channels, together with the relevant offset correction.  In the present
case the insertion and recording tabloids are both $P$, so
\[
\offset(P,P)=0.
\]
The offset correction therefore vanishes, and
\[
\rho_{r+1}-\rho_r=h_r(x).
\]
\end{proof}

\begin{lemma}[Block antisymmetry of the AMBC weight]
\label{rho-antisymmetry-lem}
One has
\[
\rho=-\rev_{\lambda}(\rho).
\]
Consequently, the sum of the coordinates of $\rho$ on each maximal
equal-part block of $\lambda$ is zero.
\end{lemma}

\begin{proof}
The inverse formula for AMBC gives, in the notation of this paper,
\[
\Phi(x^{-1})
=
\bigl(P,P,-\rev_{\lambda}(\rho)\bigr),
\]
since $\offset(P,P)=0$; see \cite[Proposition~3.1]{CLP}.
Since $x=x^{-1}$ and $\Phi$ is injective, the first identity follows.
The operator $\rev_{\lambda}$ reverses the coordinates in each maximal
block of equal parts, so the coordinates in each such block occur in
opposite pairs and have sum zero.
\end{proof}

\begin{theorem}[Recovery of the AMBC weight]
\label{rb-weight-thm}
The blockwise normalization of the raw horizontal phase vector is the AMBC
weight:
\[
\Norm_{\lambda}\bigl(\theta(x)\bigr)=\rho.
\]
In particular, the vector on the left is integral.
\end{theorem}

\begin{proof}
By Lemma~\ref{phase-altitude-lem}, the difference
$\theta_i-\rho_i$ is constant on each maximal equal-part block $I=[a,b]$.
Write
\[
\theta_i=\rho_i+c_I
\qquad(i\in I).
\]
By Lemma~\ref{rho-antisymmetry-lem},
\[
\sum_{i=a}^{b}\rho_i=0.
\]
Therefore
\[
c_I
=
\frac{1}{b-a+1}\sum_{i=a}^{b}\theta_i.
\]
Substituting this identity into $\rho_i=\theta_i-c_I$ gives exactly
\eqref{block-normalization-eq}.
\end{proof}

\begin{definition}[Affine row Beissinger datum]
\label{affine-rb-datum-def}
For $x\in\cF_n$, define
\[
\rho^{\mathrm{aff}}_{\rB}(x)
:=
\Norm_{\operatorname{shape}(P^{\mathrm{aff}}_{\rB}(x))}
\bigl(\theta(x)\bigr)
\]
and
\[
\Phi^{\mathrm{aff}}_{\rB}(x)
:=
\left(
P^{\mathrm{aff}}_{\rB}(x),
\rho^{\mathrm{aff}}_{\rB}(x)
\right).
\]
We call this pair the \defn{affine row Beissinger datum} of $x$.
\end{definition}

\begin{theorem}[Asymptotic row Beissinger realization of AMBC]
\label{affine-rb-main-thm}
Let $x\in\cF_n$.  If
\[
\Phi(x)=(P,P,\rho),
\]
then
\[
\Phi^{\mathrm{aff}}_{\rB}(x)=(P,\rho).
\]
Equivalently, the AMBC data of an affine fixed-point-free involution are
recovered from its complete two-cycle row Beissinger truncations by
\[
P_r
=
\left\{
\ol j:\row_{\BrB_N(x)}(j)=r
\text{ for all sufficiently large }N
\right\}
\]
and
\[
\rho
=
\Norm_{\lambda}\bigl(\theta(x)\bigr),
\qquad
\col_{\BrB_N(x)}(p_{r,t})
=N\lambda_r+t+\theta_r.
\]
\end{theorem}

\begin{proof}
The equality of the tabloid coordinates is
Corollary~\ref{affine-rb-tabloid-cor}, and the equality of the weight
coordinates is Theorem~\ref{rb-weight-thm}.
\end{proof}

\begin{remark}
The use of complete two-cycles is essential.  An arbitrary interval
restriction of $x$ need not be an involution, so row Beissinger insertion
would no longer be available.  Complete-cycle truncation preserves the
involution at every finite stage, while the PNAP comparison above shows
that its bulk asymptotics agree with the usual one-sided realization of
AMBC.
\end{remark}



\section{Molecules for $\Gamma^\m_n$}\label{mole-sect}
\begin{proposition}
There is a bidirected edge in the $\tilde S_n$-graph $\Gamma^\m_n$ between $x<y\in\cF_n$ if and only if there exist $\{s,t\}=\{s_{i-1},s_i\}$, such that $txt\le x<sxs=y<tyt$.
\end{proposition}
In this case, we label this bidirected edge by $\widebar i$.
\begin{proof}
By the definition, there is a bidirected edge in the $\tilde S_n$-graph $\Gamma^\m_n$ between $x<y$ if and only if $\tau_\m(x)\not\subset\tau_\m(y)$, $\tau_\m(y)\not\subset\tau_\m(x)$ and $\omega_\m(x,y)\neq0$. $\tau_\m(x)\not\subset\tau_\m(y)$ if and only if there exist $s_j\in S=\{s_1,\cdots,s_n\}$, such that $s_j\in\tau_\m(x)\setminus\tau_\m(y)$, which is equivalent to $s_jxs_j\le x<y<s_jys_j$. $\tau_\m(y)\not\subset\tau_\m(x)$ if and only if there exist $s_i\in S$, such that $s_i\in\tau_\m(y)\setminus\tau_\m(x)$, which is equivalent to $s_iys_i\le y$ and $s_ixs_i>x$. By \cite[Lemma 3.25]{Marberg}, $\omega_\m(x,y)=\delta_{s_ixs_i,y}$, so 
$\omega_\m(x,y)\neq0$ if and only if $y=sxs$. Then we have $s_jxs_j\le x<s_ixs_i= y<s_jys_j$.

Finally, we show that $|i-j|=1$ by showing that $s_is_j\neq s_js_i$. Suppose $s_is_j=s_js_i$, we have 
\[
\ell(y)<\ell(s_jys_j)=\ell(s_js_ixs_is_j)=\ell(s_is_jxs_js_i)\leq\ell(s_jxs_j)+2\leq\ell(x)+2=\ell(y),
\]
which is a contradiction.
\end{proof}


\begin{proposition}\label{bi-edge-it-prop}
There is a bidirected edge in the $\tilde S_n$-graph $\Gamma^\m_n$ between $x,y\in\cF_n$ labeled $\widebar i$ if and only if $x\simRBS{\widebar i} y$.
\end{proposition}
\begin{proof}
Without loss of generality, assume $x<y$, then there is a bidirected edge in the $\tilde S_n$-graph $\Gamma^\m_n$ between $x,y$ labeled $\widebar i$ if and only if there exist $\{s,t\}=\{s_{i-1},s_i\}$ such that $txt\le x<sxs=y<tyt$.

If $s=s_{i-1}$ and $t=s_i$, then it follows that $x(i-1)>x(i)<x(i+1)$ and $s_i(x(i-1))<s_i(x(i+1))$. 
The second inequality implies that $x(i-1) <x(i+1)$, since 
the fact that $x$ is an involution means we cannot have $x(i-1)=i+1$ and $x(i+1)=i$. 
Thus $x(i-1)$ is between $x(i)$ and $x(i+1)$, so by Theorem~\ref{direct-prop} we have $x\simRBS{\widebar i}y$.

Alternatively, if $s=s_i$ and $t=s_{i-1}$, then  $x(i+1)<x(i)>x(i-1)$ and $s_{i-1}(x(i-1))<s_{i-1}(x(i+1))$. 
The second inequality implies that $x(i-1) <x(i+1)$ since we cannot have $x(i-1)=i$ and $x(i+1)=i-1$,
so $x(i+1)$ is between $x(i)$ and $x(i+1)$. Then again by Theorem~\ref{direct-prop} we have $x\simRBS{\widebar i}y$.

For the converse statement, we see that the fixed-point-free involution $x\in\tilde{I}_n\subset$ cannot preserve the set $ \{i-1,i,i+1\}$.
Since we also assume $y\neq z$, Theorem~\ref{direct-prop} implies that either
\begin{itemize}
\item  $z=s_{i-1}ys_{i-1}$ and $y(i+1)$ is between $y(i-1)$ and $y(i)$, or
 \item $z=s_{i}ys_{i}$ and  $y(i-1)$ is between $y(i)$ and $y(i+1)$.
 \end{itemize} 
In the first case one has $s_iys_i \leq y < s_{i-1}ys_{i-1} = z <s_izs_i$ if $y(i-1) < y(i)$ or
$s_izs_i \leq z < s_{i-1}zs_{i-1} = y <s_iys_i$ if $y(i) < y(i-1)$.
Likewise,
in the second case one has $s_{i-1}ys_{i-1}\leq y < s_iys_i = z<s_{i-1}zs_{i-1}$ if $y(i) < y(i+1)$
or $s_{i-1}zs_{i-1}\leq z < s_izs_i = y<s_{i-1}ys_{i-1}$ if  $y(i+1) < y(i)$.
Either way there is a bidirected edge in the $\tilde S_n$-graph $\Gamma^\m_n$ between $x,y$ labeled $\widebar i$.
\end{proof}

Now we analyze the molecules.
\begin{theorem}\label{mol-m-thm}
Two elements $w,v\in\cF_n$ lie in the same molecule of $\Gamma^\m_n$ if and only if they are connected by a sequence of involutive transformations.
\end{theorem}
\begin{proof}
This follows from Proposition~\ref{bi-edge-it-prop}.
\end{proof}

\begin{corollary}
If two affine fixed-point-free involutions $w$ and $v$ lie in the same molecule of $\Gamma^\m_n$, then they have the same FPF sign and their AMBC images have the same shape and dominant weight.
\end{corollary}
Note that this is just a necessary condition, which is not sufficient, according to the following example.

For $n=4$, by definition of molecule, we can find such two molecules:
\[
\{[4,3,2,1],[-4,3,2,9],[3,-4,1,10],[-5,4,9,2],[4,-5,10,1],[4,11,-6,1]\}
\]
and 
\[
\{[0,-1,6,5],[0,7,-2,5],[7,0,-3,6],[-1,8,5,-2],[8,-1,6,-3],[-8,-1,6,13]\}.
\]
All these elements have FPF sign $1$, and their AMBC images have the same shape $\ytab{ \\ \\ \\ \\ }$ and the same dominant weight $\ytab{0\\0\\0\\0}$. 



\begin{theorem}\label{omega1-thm}
The molecules of $\Gamma^\m_n$ with the same shape are isomorphic to one another.
\end{theorem}
\begin{proof}
By Proposition~\ref{omega-it-prop}, if $\Gamma$ is a molecule, then $\omega\Gamma\omega^{-1}$ is also a molecule. By Theorem~\ref{omega-thm}, we see that every molecule with same weight as the molecule $\Gamma$ can be expressed as the form of $\omega^i\Gamma\omega^{-i}$. This means that every molecule with the same weight are equivalent to each other.

For two molecules with different weights, consider $x,y,x',y'$ such that $\Phi(x)=(P,P,\rho)$, $\Phi(y)=(Q,Q,\rho)$, $\Phi(x')=(P,P,\rho')$ and $\Phi(y')=(Q,Q,\rho')$. For $\rho$ and $\rho'$, define
\[
f_{\rho,\rho'}\colon
\Psi(T,T,\rho)\longmapsto\Psi(T,T,\rho').
\]
By Corollary~\ref{invol-cor}, one has
\[
\Psi(T,T,\rho)\sim_{\mathrm{It}}^{\,\widebar i}
\Psi(D_i(T),D_i(T),\rho)
\]
if and only if
\[
\Psi(T,T,\rho')\sim_{\mathrm{It}}^{\,\widebar i}
\Psi(D_i(T),D_i(T),\rho').
\]
Thus $f_{\rho,\rho'}$ is an isomorphism of labeled molecule
graphs and $x\simRBS{\widebar i} y$ if and only if $x'\simRBS{\widebar i} y'$. Thus, the two molecules containing $x$ and $x'$ are equivalent.
\end{proof}

For a partition $\lambda\vdash n$, let $\lambda'$ denote its
conjugate partition and define
\[
d_\lambda
:=
\gcd(\lambda'_1,\lambda'_2,\ldots).
\]
Equivalently, $d_\lambda$ is the greatest common divisor of the multiplicities of the distinct parts of $\lambda$.

\begin{proposition}\label{molecule-number-prop}
Let $\lambda\in\Pafpf(n)$, and let
$\rho$ be a dominant weight for which there exists
$z\in\mathcal F_n$ such that
\[
\Phi(z)=(P,P,\rho)
\qquad\text{and}\qquad
\operatorname{shape}(P)=\lambda.
\]
Then the number of molecules of $\Gamma_n^{\mathrm m}$ having
AMBC shape $\lambda$ and dominant weight $\rho$ is $d_\lambda$.

Equivalently, for any tabloid $P$ of shape $\lambda$, the least
positive integer $m$ such that $P$ and $\omega^m(P)$ are
connected by a sequence of dual equivalence operators is
$d_\lambda$.
\end{proposition}

\begin{proof}
Let $\mathcal A_\lambda$ be the graph whose vertex set is
$\operatorname{RSYT}(\lambda)$, with an edge between $T$ and
$D_i(T)$ whenever $D_i(T)\neq T$.

By Corollary~\ref{invol-cor} and Proposition~\ref{bi-edge-it-prop}, a sequence of dual
equivalence operators
\[
P=T_0,\ T_1,\ \ldots,\ T_r=Q
\]
lifts, with the weight $\rho$ fixed, to a sequence of
involutive transformations between
\[
\Psi(T_0,T_0,\rho),\,
\Psi(T_1,T_1,\rho),\,
\ldots,\,
\Psi(T_r,T_r,\rho).
\]
Conversely, every bidirected edge in $\Gamma_n^{\mathrm m}$
induces a dual equivalence edge on the corresponding AMBC
tabloids. Let $P$ be the tabloid associated with the given element $z$.
Since
\[
\Phi(\omega^r z\omega^{-r})
=
(\omega^r(P),\omega^r(P),\rho),
\]
and since the charge of $\omega^r(P)$ runs through all residue
classes modulo $d_\lambda$, every connected component of
$\mathcal A_\lambda$ contains the AMBC tabloid of an affine
fixed-point-free involution. Hence the molecules having shape $\lambda$ and weight
$\rho$ are naturally indexed by the connected components of
$\mathcal A_\lambda$.

By \cite[Theorem~8.6]{CLP}, two tabloids $T,T'$ of shape
$\lambda$ lie in the same connected component of
$\mathcal A_\lambda$ if and only if
\[
\operatorname{charge}(T)
\equiv
\operatorname{charge}(T')
\pmod{d_\lambda}.
\]
Therefore $\mathcal A_\lambda$ has exactly $d_\lambda$
connected components.

Moreover, by \cite[Lemma~8.5]{CLP},
\[
\operatorname{charge}(\omega T)
\equiv
\operatorname{charge}(T)+1
\pmod{d_\lambda}.
\]
It follows that $T$ and $\omega^m(T)$ are in the same connected
component if and only if
\[
m\equiv0\pmod{d_\lambda}.
\]
Thus the least positive such $m$ is $d_\lambda$.
\end{proof}

For $\lambda\in\Pafpf(n)$, we define
\[
\mathrm{mo}(\lambda):=d_\lambda
\]
and call this integer the \emph{molecule order} of $\lambda$.  Recall that $a|b$ means $a$ divides $b$ for two integers $a,b$. Moreover, $a|b|c$ means $a|b$ and $b|c$.



\begin{proposition}
For every
$\lambda\in\Pafpf(n)$, one has $2\mid\mathrm{mo}(\lambda)\mid n$. Specifically, we have $\mathrm{mo}((1,1,\cdots,1))=n$ and $\mathrm{mo}((\frac{n}{2},\frac{n}{2}))=2$.
\end{proposition}
\begin{proof}
By Proposition~\ref{molecule-number-prop},
\[
\mathrm{mo}(\lambda)
=
d_\lambda
=
\gcd(\lambda'_1,\lambda'_2,\ldots).
\]
Since
\[
n=\sum_j\lambda'_j,
\]
the integer $d_\lambda$ divides $n$. Hence
\[
\mathrm{mo}(\lambda)\mid n.
\]

We next show that $\mathrm{mo}(\lambda)$ is even.
Suppose, to the contrary, that
$m:=\mathrm{mo}(\lambda)$ is odd. Choose
$z\in\mathcal F_n$ with
\[
\Phi(z)=(P,P,\rho)
\qquad\text{and}\qquad
\operatorname{shape}(P)=\lambda.
\]
By the definition of $\mathrm{mo}(\lambda)$, the tabloids
$P$ and $\omega^m(P)$ are connected by a sequence of dual
equivalence operators. Corollary~3.8 and Proposition~4.1 then
imply that $z$ and
\[
\omega^m z\omega^{-m}
\]
are connected by a sequence of involutive transformations.

On the other hand, conjugation by $\omega$ interchanges the two
FPF conjugacy classes $\mathcal F_n^+$ and $\mathcal F_n^-$.
Since $m$ is odd, $z$ and $\omega^m z\omega^{-m}$ have opposite
FPF signs. This contradicts the fact that involutive
transformations preserve the FPF conjugacy class. Therefore
\[
2\mid\mathrm{mo}(\lambda).
\]

Finally, if $\lambda=(1^n)$, then
\[
\lambda'=(n),
\]
so
\[
\mathrm{mo}(1^n)=d_{(1^n)}=n.
\]
If
\[
\lambda=\left(\frac n2,\frac n2\right),
\]
then
\[
\lambda'
=
(\underbrace{2,2,\ldots,2}_{\frac n2\text{ parts}}),
\]
and consequently
\[
\mathrm{mo}
\left(\frac n2,\frac n2\right)
=
d_{(\frac n2,\frac n2)}
=
2.
\]
\end{proof}

\begin{proposition}
\label{minimal-orbit-prop}
Let $\Gamma$ be a molecule of $\Gamma_n^{\mathrm m}$ of
AMBC shape $\lambda\in\Pafpf(n)$,
and let
\[
d_\lambda=\gcd(\lambda'_1,\lambda'_2,\ldots).
\]
Then the set of minimal elements of $\Gamma$ is invariant under
the map
\[
z\longmapsto \omega^{d_\lambda}z\omega^{-d_\lambda}.
\]
In particular, if $\Gamma$ has a unique minimal element, then
\[
\frac{n}{d_\lambda}\mid\lambda_i
\qquad\text{for every part $\lambda_i$ of $\lambda$.}
\]
\end{proposition}

\begin{proof}
By Proposition~\ref{molecule-number-prop}, conjugation by
$\omega^{d_\lambda}$ preserves every molecule of AMBC shape
$\lambda$. This conjugation is induced by an automorphism of the
affine Coxeter diagram, and therefore preserves the FPF Bruhat
order. Hence it permutes the minimal elements of $\Gamma$.

Suppose that $\Gamma$ has a unique minimal element $z$, and
write
\[
\Phi(z)=(P,P,\rho).
\]
The uniqueness of $z$ implies
\[
\omega^{d_\lambda}z\omega^{-d_\lambda}=z.
\]
By Proposition~4.1 and the injectivity of $\Phi$, it follows
that
\[
\omega^{d_\lambda}(P)=P.
\]

The permutation of $\mathbb Z/n\mathbb Z$ given by
$x\mapsto x+d_\lambda$ has orbits of size
\[
\frac{n}{d_\lambda}.
\]
Since every row of $P$ must be invariant under this
permutation, every row is a union of such orbits. Consequently,
\[
\frac{n}{d_\lambda}\mid |P_i|=\lambda_i
\]
for every row $P_i$.
\end{proof}

\begin{remark}
The preceding divisibility condition need not hold for every $\lambda\in\Pafpf(n)$. For example, if
\[
\lambda=(2,2,1,1)\vdash6,
\]
then $\lambda'=(4,2)$ and $d_\lambda=2$, while
$n/d_\lambda=3$ divides none of the parts of $\lambda$.
Therefore no molecule of this shape has a unique minimal
element. 

A direct computation using Theorem~\ref{direct-prop} shows that
the molecule containing
\[
z=[3,6,1,5,4,2]
\]
has exactly three minimal elements, namely $[0,-1,4,3,8,7],[2,1,6,5,4,3],[4,3,2,1,6,5]$.
\end{remark}




 \begin{thebibliography}{99}
%\bibitem{Assaf} S. Assaf, Dual Equivalence Graphs I: A new paradigm for Schur positivity, \emph{Forum of Mathematics, Sigma}, \textbf{3}, E12.

\bibitem{APR} R. Adin, A. Postnikov, and Y. Roichman, Combinatorial Gelfand models, \emph{J. Algebra} \textbf{320} (2008), 1311--1325. 
 
\bibitem{Beissinger} J. S. Beissinger, Similar constructions for Young tableaux and involutions, and their application to shiftable tableaux, \emph{Discrete Math.} \textbf{67} (1987), 149--163.

\bibitem{CFKLY} M. Chmutov, G. Frieden, D. Kim, J.B. Lewis, and E. Yudovina, An affine generalization of evacuation, \emph{Sel. Math. New Ser.} \textbf{28}, 67 (2022).

\bibitem{CLP} M. Chmutov, J. B. Lewis and P. Pylyavskyy, Monodromy in Kazhdan-Lusztig cells in affine type A, \emph{Math. Annalen} \textbf{386} (2022), 1891--1949.

\bibitem{CPY} M. Chmutov, P. Pylyavskyy and E. Yudovina, Matrix-Ball Construction of affine Robinson-Schensted correspondence, \emph{Sel. Math. New Ser.} \textbf{24} (2018), 667--750.


\bibitem{EG} P. Edelman and C. Greene, Balanced tableaux, \emph{Adv. in Math.} \textbf{63(1)}(1987), 42--99.

\bibitem{Fulton} W. Fulton, Young Tableaux With Applications to Representation Theory and Geometry, London Mathematical Society Student Texts \textbf{35} (1997), Cambridge University Press.

%\bibitem{Garfinkle1} D. Garfinkle, On the classification of primitive ideals for complex classical Lie algebras (I), \emph{Compositio Math.}, \textbf{75(2)}:135–169, 1990.

%\bibitem{Garfinkle2} D. Garfinkle, On the classification of primitive ideals for complex classical Lie algebras (II), \emph{Compositio Math.}, \textbf{81(3)}:307–336, 1992.

%\bibitem{GeckPfeiffer} M. Geck and G. Pfeiffer, \emph{Characters of finite Coxeter groups and Iwahori-Hecke algebras}, Oxford University Press, 2000.

%\bibitem{Gyoja} A. Gyoja, On the existence of a $W$-graph for an irreducible representation of a Coxeter group, \emph{J. Algebra} \textbf{86} (1984), 422--438.


%\bibitem{HowlettYin} R. B. Howlett and Y. Yin, Inducing $W$-Graphs,
%\emph{Math. Z.} \textbf{244} (2003) 415--431.
%
%\bibitem{HowlettYin2} R. B. Howlett and Y. Yin, Inducing $W$-graphs II, \emph{Manuscripta Mathematica} \textbf{115} (2004), no. 4, 495--511.


%\bibitem{IRS}{N. F. J. Inglis, R. W. Richardson, and J. Saxl}, An explicit model for the complex representations of $S_n$, \emph{Arch. Math. (Basel)} \textbf{54} (3) (1990) 258--259.

\bibitem{KL} D. Kazhdan and G. Lusztig, Representations of Coxeter groups and Hecke algebras, \emph{Invent. Math.} \textbf{53} (1979), 165--184.

\bibitem{KP} D. Kim, P. Pylyavskyy, Asymptotic Hecke algebras and Lusztig-Vogan bijection via affine matrix-ball construction, \emph{Int. Math. Res. Notices} \textbf{18} (2023), 16051--16103.

%\bibitem{KV2004} V. Kodiyalam and D. N. Verma, A natural representation model for symmetric groups, preprint (2004), {\tt arXiv:math/0402216v1}.

%\bibitem{Knuth} D.E. Knuth. Permutations, matrices, and generalized Young tableaux, \emph{Pacific Journal of Mathematics} \textbf{34} (1970), 709–727.

%\bibitem{LV} G. Lusztig and D. A. Vogan, Hecke algebras and involutions in Weyl groups, 
%\emph{Bull. Inst. Math. Acad. Sinica (N.S.)} \textbf{7} (2012), 323-354.
%
%\bibitem{LV2} G. Lusztig, A bar operator for involutions in a Coxeter group, \emph{Bull. Inst. Math. Acad. Sinica
%(N.S.)} \textbf{7} (2012), 355--404.

%\bibitem{M2011} E. Marberg, Automorphisms and generalized involution models of finite complex reflection groups, 
%\emph{J.  Algebra} \textbf{334} (2011), 295--320.
%
%\bibitem{M2012} E. Marberg, Generalized involution models for wreath products, \emph{Israel J. Math.} \textbf{192} (2012), 157--195.
%
%\bibitem{M2014} E. Marberg, Comparing and characterizing some constructions of canonical bases from Coxeter systems, \emph{Representations of Reductive Groups},
%Progress in Mathematics \textbf{312} (2015), 399--436.

\bibitem{Marberg} E. Marberg, Bar operators for quasiparabolic conjugacy classes in a Coxeter group,
\emph{J. Algebra} \textbf{453} (2016), 325--363.

\bibitem{Marberg2} E. Marberg, On some actions of the $0$-Hecke monoids of affine symmetric groups, 
\emph{J. Combin. Theory Ser. A} \textbf{161} (2019), 178--219.

\bibitem{MZ} E. Marberg and Y. Zhang, Gelfand $W$-graphs for classical Weyl groups, \emph{J. Algebra} \textbf{609} (2022), 292--336.

\bibitem{MZ2} E. Marberg and Y. Zhang, Perfect models for finite Coxeter groups, \emph{J. Pure Appl. Algebra} \textbf{227} (2023), 107303.

\bibitem{MZ3} E. Marberg and Y. Zhang, Insertion algorithms for Gelfand $S_n$-graphs, \emph{Ann. Combin.} \textbf{28} (2024), 1199--1242.

%\bibitem{Post} J. Post, Combinatorics of arc diagrams, Ferrers fillings, Young tableaux and lattice paths, Msc Thesis, Simon Fraser University (2009)

\bibitem{RV} E. M. Rains and M. J. Vazirani, Deformations of permutation representations of Coxeter groups, \emph{J. Algebr. Comb.} \textbf{37} (2013), 455--502. 

\bibitem{Shi} J.-Y. Shi, The Kazhdan-Lusztig Cells in Certain Affine Weyl Groups, \emph{Lecture Notes in Math.},\textbf{1179}, Springer-Verlag, (1986).

\bibitem{Shi2} J.-Y. Shi, The generalized Robinson-Schensted algorithm on the affine Weyl group of type $\tilde{{A}}_{n-1}$, \emph{J. Algebra} \textbf{139} (1991), 364--394.


%\bibitem{StanleyPKernel} R. P. Stanley, Subdivisions and local $h$-vectors, \emph{J. Amer. Math. Soc.} \textbf{5} (1992) 805--851.

\bibitem{Stanley} R. P. Stanley, Enumerative Combinatorics, Volume II. Cambridge Univ. Press, Cambridge, (1999).

\bibitem{Stem1} J. R. Stembridge,  Admissible $W$-graphs, \emph{Represent. Theory} \textbf{12} (2008), 346--368.

\bibitem{Stem2} J. R. Stembridge,  Admissible $W$-graphs and commuting Cartan matrices, \emph{Adv. in Appl. Math.} \textbf{44} (2010), 203--224.

%\bibitem{Schensted} C. Schensted, Longest increasing and decreasing subsequences, \emph{Canadian Journal of Mathematics} \textbf{13}(1961), 179--191.

\bibitem{Viennot} G.~Viennot, Une forme g{\'e}om{\'e}trique de la correspondance de Robinson-Schensted, \emph{Combinatoire et repr{\'e}sentation du groupe sym{\'e}trique} (1977), 29--58.

\bibitem{Vogan} D. A. Vogan, A generalized {$\tau$}-invariant for the primitive spectrum of a semisimple Lie algebra, \emph{Math. Ann.} \textbf{242} (1979), 209--224.

%\bibitem{Webster} B. Webster, Canonical bases and higher representation theory, \emph{Compos. Math.} \textbf{151} (2015), 121--166.

\bibitem{Zhang} Y. Zhang, Quasiparabolic sets and affine fixed-point-free involutions, \emph{J. Algebra} \textbf{587} (2021), 522-554.


\end{thebibliography}
\end{document}