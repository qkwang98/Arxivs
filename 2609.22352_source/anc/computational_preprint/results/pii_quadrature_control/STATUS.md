# Partial Calibration

The comparison at contour tolerance `1e-12` completed, including its
matched Runge-Kutta trial. These records and original timings are reusable.

The next level, `1e-15`, gave a contour error near `5.6e-23`. Its RK matching
run was interrupted to avoid an unnecessarily expensive calibration. This
level and the requested `1e-18` level did not complete and are not results.
The final residual and integrity checks in this directory did not run.
The completed follow-up is stored separately in `../pii_comparison/`.
